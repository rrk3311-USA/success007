# Mobile image sizes (~400px)

Use these images for:

- Product page main image on mobile (below 768px)
- Product page thumbnails (small grid)
- Any small product image on mobile

**Target:** ~400×400 px (or same aspect as source). Mirror the structure under `images/products/` (e.g. `52274-401/01.png`).

Large images are used only for the desktop/tablet product page hero.
