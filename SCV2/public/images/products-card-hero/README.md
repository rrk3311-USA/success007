# Card / hero image sizes (750px)

Use these images for:

- Product cards (store grid)
- Bundles, upsell, recently viewed
- Any listing where the image is shown at card size

**Target:** 750×750 px (or same aspect as source). Mirror the structure under `images/products/` (e.g. `52274-401/01.png`).

Large (current) images in `images/products/` are used **only** for the desktop/tablet product page main image.
